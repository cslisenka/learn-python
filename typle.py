# Tuples and operations
coordinates = (4, 5) # tuple with 2 values, it is immutable, we can't change values
print(coordinates)
print(coordinates[0]) # access to element 0
print(coordinates[1]) # access to element 1